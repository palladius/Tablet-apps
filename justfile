
list:
  just -l

run:
  bundle exec rackup -o 0.0.0.0 -p 9292

  
  # intallrspec 

list:
  just -l

run:
  bundle exec rackup -o 0.0.0.0 -p 9292

test:
  rspec

  # intallrspec 
  # bundle exec rspec --init
  